const { generateAutoResponse } = require('./src/lib/autoResponder.js'); // Actually, typescript. I should write it in JS and use ts-node or just use the webhook.

// Wait, I can just use `npm run dev` and send a test webhook using curl or fetch.
